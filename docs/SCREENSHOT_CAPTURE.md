# Screenshot capture checklist · 60 seconds

I need 4 screenshots to finish the README + submission package. Capture them in any browser at desktop width and drop them into `docs/screenshots/` — I'll wire them in.

## Setup

1. Open Chrome / Edge / Brave at **1440 × 900** or **1920 × 1080** viewport (avoid mobile width).
2. Light mode (the editorial paper palette is the front door).
3. Use the browser's full-page screenshot:
   - **Chrome/Edge**: `Ctrl+Shift+P` → type "screenshot" → "Capture full size screenshot"
   - **Firefox**: right-click → "Take Screenshot" → "Full Page"
   - **Or Snipping Tool**: `Win+Shift+S`, drag the relevant region

## Captures needed

| Filename | URL | What to capture |
|---|---|---|
| `docs/screenshots/watch-cinematic.gif` | <https://hermesgenesis.world/watch> | The cinematic mode mid-playback. Use a **GIF recorder** (LICEcap / ScreenToGif / Windows + G) — capture **6–8 seconds** including a scene image fade and a narration line on screen. ~2 MB target. |
| `docs/screenshots/glossary.png` | <https://hermesgenesis.world/glossary> | Full page. The alphabetical glossary with the "linguistic family tree" caption + a few terms visible (Lunar/Cinder/Ash columns). |
| `docs/screenshots/lexicon-preview.png` | <https://hermesgenesis.world> (scroll down past Hero) | The "The language drifts as the world ages." section with the 3 morphing English terms. |
| `docs/screenshots/nav-with-freshness.png` | any page | **Just the masthead** — wordmark + freshness pill ("live · Nm ago") + 8-item nav (chronicle · watch · glossary · regen · demo · control · judge · method). Crop to ~1920 × 100 px. |

## After you save them

Tell me — I'll commit them, update the README's grid, and that's it.

## Why this matters for the submission

- `/watch` is our most spectacular surface. The current README has zero visual of it.
- `/glossary` is the conlang differentiator vs AI Town. Judges need to *see* it to value it.
- `lexicon-preview` shows we surface the differentiator on the front door, not just hide it behind a route.
- The 8-item nav with freshness pill is the one frame that says "this is a real publication, currently publishing." Worth more than another shot of an article.

5 minutes of capture work. I'll do everything downstream.
